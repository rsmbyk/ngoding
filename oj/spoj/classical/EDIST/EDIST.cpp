#include <iostream>
using namespace std;

int edist (string str1, string str2, int i1, int i2) {
    
    if (str1[i1] != str2[i2]) {
        
    }
}

int main (int argc, char const *argv[]) {
    int tc;
    cin >> tc;
    
    while (tc--) {
        string str1, str2;
        cin >> str1 >> str2;
        
        int changes, i1, i2, ;
        cout << edist (str1, str2, 0, 0) << endl;
    }
    
    return 0;
}